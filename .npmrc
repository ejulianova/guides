# Keep git tags consistent with v1.x's convention (bare "1.2.9", not "v1.2.9").
# Without this, `npm version` (used by the release scripts in package.json)
# defaults to prefixing tags with "v".
tag-version-prefix=
